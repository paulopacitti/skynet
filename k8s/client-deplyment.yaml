apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
  namespace: default
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  selector:
    matchLabels:
      app: client
  strategy:
    rollingUpdate:
      maxSurge: 33%
      maxUnavailable: 33%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: client
    spec:
      containers:
      - image: ghcr.io/paulopacitti/skynet-client:main
        imagePullPolicy: IfNotPresent
        name: client
        resources:
          requests:
            memory: "256Mi"
            cpu: "250m"
          limits:
            memory: "512Mi"
            cpu: "350m"
        ports:
        - containerPort: 50001
          protocol: TCP